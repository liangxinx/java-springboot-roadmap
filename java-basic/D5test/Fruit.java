package D5test;
//建立一個 `Fruit` class，屬性有 `name`、`price`。
// 在 `Main` 裡建立一個 `Fruit[]` 陣列，裡面放 3 個不同的水果物件，用 `for` 迴圈把每個水果的名稱和價格印出來。
public class Fruit {
    String name;
    int price;

    public Fruit(String name,int price){
        this.name=name;
        this.price=price;
    }
}


